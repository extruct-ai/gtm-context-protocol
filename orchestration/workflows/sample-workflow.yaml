id: workflow.sample-workflow
kind: workflow
name:
version: 1
status: draft

trigger:
  type: # schedule | event | manual
  schedule:

scope:
  source:
    asset: # e.g. campaign.sample-campaign.companies

steps:
  - id:
    action: # read | prompt | append | workflow
    assets: []
