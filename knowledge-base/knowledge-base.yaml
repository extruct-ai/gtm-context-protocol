id: kb
kind: knowledge-base
name:
version: 1
status: draft

components:
  definition:
    id: kb.definition
    path: ./definition.md
    format: markdown

  signals:
    id: kb.signals
    path: ./signals/
    format: directory

  product:
    id: kb.product
    path: ./product/
    format: directory

  personas:
    id: kb.personas
    path: ./personas/
    format: directory

  use_cases:
    id: kb.use-cases
    path: ./use-cases/
    format: directory

  case_studies:
    id: kb.case-studies
    path: ./case-studies/
    format: directory

  objections:
    id: kb.objections
    path: ./objections/
    format: directory

  competitors:
    id: kb.competitors
    path: ./competitors/
    format: directory

links:
  campaigns: []

orchestration:
  workflows: []
